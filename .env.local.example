# Copy this file to .env.local and fill in your values.
# .env.local is git-ignored and never committed.

# Required for local mode (npm run local).
# Create an Entra SPA app registration — see README § "Local mode" for steps.
VITE_LOCAL_CLIENT_ID=paste-your-app-client-id-here
